# Worktrees
.worktrees/

# Node
node_modules/
dist/

# Env
.env
.env.*

# Local dev config (contains secrets)
dev/

# Logs
*.log
logs/

# Local incident history
.dops/

# Per-stack data (services.yaml, history)
data/

# Design/plan docs (internal)
docs/plans/
docs/superpowers/
TODOS.md

# Claude Code
.claude/

# Superpowers brainstorm sessions (local UI artifacts)
.superpowers/

# Docker dev compose files (contain env/secrets)
docker-compose.dev*.yml

# Local binaries/scripts
bin/

# SQLite
*.sqlite
*.sqlite-shm
*.sqlite-wal

# Benchmarks (local output) — keep playbook + scripts committed, ignore results
benchmark/*
!benchmark/README.md
!benchmark/run-ab-eval.sh
!benchmark/analyze-ab.py
.gstack/

# Playwright artifacts
playwright-report/
test-results/
playwright/.cache/
.playwright-mcp/

# Git hooks are per-developer setup, not shared
.githooks/

# Local demo seed output (DB + generated services.yaml/providers.yaml).
# Never commit — the seed script is the source of truth; `npm run seed:demo`
# regenerates this. Keep out of history to avoid massive diffs on every reseed.
data-demo/
